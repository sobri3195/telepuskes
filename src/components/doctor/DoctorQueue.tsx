import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';import { Textarea } from '@/components/ui/textarea';import { Button } from '@/components/ui/button';import { useConsultationStore } from '@/stores/consultationStore';import { TriageBadge } from '@/components/patient/TriageBadge';export function DoctorQueue(){const cs=useConsultationStore(s=>s.consultations);const update=useConsultationStore(s=>s.updateConsultation);return <div className="grid gap-4 xl:grid-cols-2">{cs.map(c=><Card key={c.id}><CardHeader><CardTitle className="flex justify-between text-base"><span>{c.patientName}</span><TriageBadge priority={c.priority}/></CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm">{c.title} — {c.summary}</p><Textarea placeholder="Catatan SOAP simulasi" defaultValue={c.soap}/><div className="grid gap-2 sm:grid-cols-2"><Textarea placeholder="Resep simulasi" defaultValue={c.prescription}/><Textarea placeholder="Rujukan simulasi" defaultValue={c.referral}/></div><Button onClick={()=>update(c.id,{status:'Aktif',soap:'S: keluhan dicatat. O: stabil. A/P: edukasi dan monitoring.'})}>Simpan catatan</Button></CardContent></Card>)}</div>}
