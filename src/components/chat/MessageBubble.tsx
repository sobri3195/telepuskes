import type { Message } from '@/types';import { formatTime } from '@/lib/utils';export function MessageBubble({m}:{m:Message}){const mine=m.sender==='patient';return <div className={`flex ${mine?'justify-end':'justify-start'}`}><div className={`max-w-[82%] rounded-2xl px-4 py-2 text-sm ${mine?'bg-primary text-white':'bg-slate-100 text-slate-800'}`}><p>{m.text}</p>{m.attachmentName&&<p className="mt-1 text-xs underline">📎 {m.attachmentName}</p>}<p className="mt-1 text-[10px] opacity-70">{formatTime(m.timestamp)} · {m.status}</p></div></div>}
