import { Button } from '@/components/ui/button';import { csvEscape } from '@/lib/utils';import { useConsultationStore } from '@/stores/consultationStore';export function ReportFilter(){const cs=useConsultationStore(s=>s.consultations);function exportCsv(){const header=['id','patientName','title','priority','status','createdAt'];const csv=[header.join(','),...cs.map(c=>header.map(h=>csvEscape((c as unknown as Record<string,unknown>)[h])).join(','))].join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='laporan-telehealth-au-dummy.csv';a.click();}return <div className="flex flex-wrap items-center gap-2 rounded-xl border bg-white p-4"><span className="text-sm font-medium">Laporan dummy frontend-only</span><Button onClick={exportCsv}>Export CSV</Button></div>}
