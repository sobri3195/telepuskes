import { Bell, LogOut, Search } from 'lucide-react';import { useAuthStore } from '@/stores/authStore';import { Input } from '@/components/ui/input';import { Button } from '@/components/ui/button';export function TopHeader(){const user=useAuthStore(s=>s.user);const logout=useAuthStore(s=>s.logout);return <header className="sticky top-0 z-30 border-b bg-white/90 backdrop-blur"><div className="flex h-16 items-center gap-3 px-4 lg:px-6"><div className="lg:hidden"><b>Telehealth AU</b></div><div className="relative hidden max-w-md flex-1 md:block"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Cari konsultasi, faskes, satuan..."/></div><div className="ml-auto flex items-center gap-3"><Bell className="h-5 w-5 text-slate-500"/><div className="hidden text-right sm:block"><p className="text-sm font-semibold">{user?.name??'Guest'}</p><p className="text-xs text-muted-foreground">{user?.role??'Simulasi'}</p></div><Button size="sm" variant="outline" onClick={logout}><LogOut className="h-4 w-4"/></Button></div></div></header>}
