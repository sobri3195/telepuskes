import { NavLink } from 'react-router-dom';import { Building2, HeartPulse, Home, MessageSquare, User } from 'lucide-react';const items=[['/app/dashboard',Home,'Home'],['/app/needs',HeartPulse,'Butuh'],['/app/messages',MessageSquare,'Pesan'],['/app/units',Building2,'Faskes'],['/app/profile',User,'Profil']] as const;export function MobileBottomNavigation(){return <nav className="fixed inset-x-0 bottom-0 z-40 grid grid-cols-5 border-t bg-white lg:hidden">{items.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 py-2 text-xs ${isActive?'text-primary':'text-slate-500'}`}><Icon className="h-5 w-5"/>{label}</NavLink>)}</nav>}
