import { useMemo, useState } from 'react';import { Search } from 'lucide-react';import { Card, CardContent } from '@/components/ui/card';import { Input } from '@/components/ui/input';import { Select } from '@/components/ui/select';import { organizationRoots, units, facilities } from '@/data/mockData';import type { Unit } from '@/types';import { UnitDetail } from './UnitDetail';
export function UnitDirectory(){const [q,setQ]=useState('');const [kotama,setKotama]=useState('all');const [selected,setSelected]=useState<Unit>();const all=[...units,...facilities];const filtered=useMemo(()=>all.filter(u=>(kotama==='all'||u.kotamaInduk===kotama)&&(u.nama.toLowerCase().includes(q.toLowerCase())||u.kota.toLowerCase().includes(q.toLowerCase())||u.jenis.toLowerCase().includes(q.toLowerCase()))),[q,kotama]);return <div className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]"><div className="space-y-4"><div className="flex flex-col gap-2 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Cari Lanud, Koopsud, RSAU/RSPAU, klinik..." value={q} onChange={e=>setQ(e.target.value)}/></div><Select value={kotama} onChange={e=>setKotama(e.target.value)}><option value="all">Semua kotama</option>{organizationRoots.map(r=><option key={r}>{r}</option>)}</Select></div><div className="grid gap-3 md:grid-cols-2">{filtered.slice(0,30).map(u=><Card key={u.id} onClick={()=>setSelected(u)} className="cursor-pointer hover:border-primary"><CardContent className="pt-5"><b>{u.nama}</b><p className="text-sm text-muted-foreground">{u.jenis} · {u.kotamaInduk}</p><p className="text-xs">{u.kota}, {u.provinsi}</p></CardContent></Card>)}</div></div><UnitDetail unit={selected}/></div>}
