import { Card } from '@/components/ui/card';import type { PatientNeedType } from '@/types';export function NeedSelectionCard({type,onSelect}:{type:PatientNeedType;onSelect:(t:PatientNeedType)=>void}){return <button onClick={()=>onSelect(type)} className="text-left"><Card className="h-full p-4 transition hover:-translate-y-0.5 hover:border-primary hover:shadow-md"><p className="font-semibold text-slate-800">{type}</p><p className="mt-2 text-xs text-muted-foreground">Mulai alur {type.toLowerCase()}.</p></Card></button>}
