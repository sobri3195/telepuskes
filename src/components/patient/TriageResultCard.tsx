import { useNavigate } from 'react-router-dom';import { Button } from '@/components/ui/button';import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';import type { TriageResult } from '@/types';import { facilities } from '@/data/mockData';import { TriageBadge } from './TriageBadge';
export function TriageResultCard({result,chatId}:{result:TriageResult;chatId?:string}){const nav=useNavigate();const f=facilities.find(x=>x.id===result.facilityId)||facilities[0];return <Card className="border-primary/30"><CardHeader><CardTitle className="flex items-center justify-between">Hasil triase <TriageBadge priority={result.priority}/></CardTitle></CardHeader><CardContent className="space-y-3"><p>{result.reason}</p><p className="font-medium">Rekomendasi: {result.recommendation}</p><p className="rounded-lg bg-sky-50 p-3 text-sm">Faskes rekomendasi: <b>{f.nama}</b> — {f.kota}, {f.nomorKontak}</p><Button onClick={()=>nav(chatId?`/app/messages/${chatId}`:'/app/messages')}>Mulai chat</Button></CardContent></Card>}
