import { create } from 'zustand';import { persist } from 'zustand/middleware';type S={sidebarCollapsed:boolean;setSidebarCollapsed:(v:boolean)=>void;search:string;setSearch:(v:string)=>void};export const useUiStore=create<S>()(persist(set=>({sidebarCollapsed:false,setSidebarCollapsed:v=>set({sidebarCollapsed:v}),search:'',setSearch:v=>set({search:v})}),{name:'telehealth-au-ui'}));
