import { Link, useParams } from 'react-router-dom';import { Card, CardContent } from '@/components/ui/card';import { ChatRoom } from '@/components/chat/ChatRoom';import { useChatStore } from '@/stores/chatStore';export function ChatPage(){const {id}=useParams();const chats=useChatStore(s=>s.chats);const chat=chats.find(c=>c.id===id)||chats[0];if(!id)return <div className="grid gap-3 md:grid-cols-2">{chats.map(c=><Link key={c.id} to={`/app/messages/${c.id}`}><Card className="hover:border-primary"><CardContent className="pt-5"><b>{c.title}</b><p className="text-sm text-muted-foreground">{c.messages.at(-1)?.text}</p></CardContent></Card></Link>)}</div>;return chat?<ChatRoom chat={chat}/>:<p>Chat belum tersedia.</p>}
