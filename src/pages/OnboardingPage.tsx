import { useNavigate } from 'react-router-dom';import { Button } from '@/components/ui/button';import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';import { Select } from '@/components/ui/select';import { organizationRoots, units, facilities } from '@/data/mockData';export function OnboardingPage(){const nav=useNavigate();return <div className="mx-auto max-w-3xl space-y-4"><h1 className="text-3xl font-bold">Onboarding Pasien</h1><Card><CardHeader><CardTitle>Lengkapi preferensi layanan</CardTitle></CardHeader><CardContent className="space-y-4"><Select><option>Personel aktif</option><option>Keluarga besar TNI AU</option><option>Purnawirawan</option></Select><Select>{organizationRoots.map(x=><option key={x}>{x}</option>)}</Select><Select>{units.slice(0,20).map(x=><option key={x.id}>{x.nama}</option>)}</Select><Select>{facilities.map(x=><option key={x.id}>{x.nama}</option>)}</Select><Button onClick={()=>nav('/app/dashboard')}>Selesai</Button></CardContent></Card></div>}
